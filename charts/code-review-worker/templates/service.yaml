apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-review-worker.fullname" . }}
  labels:
    {{- include "code-review-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "code-review-worker.selectorLabels" . | nindent 4 }}
